{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DBConnection": "Server=.;Database=EHREndProject;Trusted_Connection=True;MultipleActiveResultSets=true; TrustServerCertificate=True;Encrypt=True;"
  },
  //These are values that we’ll need when creating the tokens.
  "JwtSettings": {
    // specify which entity is creating the token (i.e. the “Issuer”) المصدر
    "validIssuer": "EHRAPI",
    "Secret": "* EHR # PROJECT # JWT ^ SECRET ^ KEY *",
    //specify which entity is the token intended for (i.e. the “Audience”)
    "validAudience": "https://localhost:7200",
    "expires": 30
  },
  "EmailConfiguration": {
    "From": "EgyptEHRApp.com",
    "SmtpServer": "smtp.gmail.com",
    "Port": 465,
    "Username": "EgyptEHRApp@gmail.com",
    "Password": "tlofqcbednbviqhq"
  },

  "AllowedHosts": "*"
}
